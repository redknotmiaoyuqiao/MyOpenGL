attribute vec3 vPosition;
uniform vec4 vColor;

varying vec4 f_color;

void main(){
    float x = vPosition.x;
    float y = vPosition.y;
    float z = vPosition.z;

    x = x;
    y = y;

    gl_Position = vec4(x,y,z,1.0);

    f_color = vec4(x, y, z,1.0);
}